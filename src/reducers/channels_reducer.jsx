import channel from '../../data/channels';

export default function(state = channel) {
  // switch (action.type) {
  //   case 'SET_CHANNEL':
  //     return action.payload;
  //   default:
  return state;
  // }
}
